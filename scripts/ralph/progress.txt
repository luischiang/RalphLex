# Ralph Progress Log
Started: Sat Mar 28 02:34:44 PM -05 2026
---

## Codebase Patterns
- Python venv at `.venv/` — use `.venv/bin/` prefix for all Python tools (pytest, ruff, mypy, uvicorn)
- LegalStore uses sync sqlite3 (not aiosqlite) — simpler and sufficient for this use case
- For httpx async client mocking: use `MagicMock` for response (json/raise_for_status are sync), `AsyncMock` for client.get and context manager methods
- `_make_httpx_mock()` helper in test_legal_db.py creates properly wired httpx.AsyncClient mock
- `pyproject.toml` uses setuptools with explicit package find: `include = ["backend*", "tests*"]`
- Backend runs on port 8000, frontend on port 3000 with Vite proxy for `/api` to backend
- Tailwind CSS v4 via `@tailwindcss/vite` plugin — use `@import "tailwindcss"` in index.css
- Ruff for linting/formatting, mypy for typechecking, pytest with asyncio mode auto
- Frontend uses Vite 6+ template — no separate tailwind.config.js needed with v4
- Use `datetime.now(UTC)` instead of `datetime.utcnow()` — the latter is deprecated in Python 3.12+
- Models are in `backend/models/case.py`, re-exported from `backend/models/__init__.py`
- CaseFolderManager in `backend/services/case_folder.py` — use `tmp_path` fixture in tests for isolation
- mypy `warn_return_any` is on — annotate return types from `json.loads()` explicitly (e.g., `dict[str, object]`)
- API routers registered in `backend/app.py` with `prefix="/api"` — add new routers there
- Use `unittest.mock.patch` on module-level `folder_manager` to inject tmp_path-based CaseFolderManager in tests
- FastAPI TestClient (sync) is sufficient for testing — no need for httpx AsyncClient
- `backend/config.py` uses pydantic-settings `Settings` class — import `settings` singleton for env vars
- LLMAgent in `backend/agents/base.py` — pass `client=mock_client` in tests to inject mocked Anthropic client
- Use `asyncio.to_thread(lambda: client.messages.create(...))` to wrap sync Anthropic SDK for async
- mypy needs `cast(list[MessageParam], messages)` for Anthropic SDK message types
- Anthropic SDK content blocks are union types — use `hasattr(block, "text")` guard + `type: ignore[union-attr]`
- ClaimantAgent/RespondentAgent extend LLMAgent — use `generate_argument()` with case_facts, iteration, opponent_argument
- Convergence detection via `difflib.SequenceMatcher` — threshold 0.85 by default, weighted across argument fields
- ArgumentLoop orchestrator alternates claimant/respondent — persists each round to iterations/round_{n}_{role}.json
- For tests needing multiple sequential LLM responses, use `client.messages.create.side_effect = [resp1, resp2, ...]`

---

## 2026-03-28 - RLEX-001
- Implemented full project scaffolding with FastAPI backend and React + Vite + TypeScript + Tailwind CSS frontend
- Files created:
  - `pyproject.toml` — project config with all dependencies and tool settings
  - `backend/app.py` — FastAPI app with CORS middleware
  - `backend/api/health.py` — health check endpoint returning `{"status": "ok"}`
  - `frontend/` — Vite + React + TypeScript scaffold with Tailwind CSS v4
  - `frontend/vite.config.ts` — configured port 3000, API proxy to port 8000
  - `Makefile` — dev-backend, dev-frontend, dev, install, lint, test, typecheck targets
  - `.env.example` — ANTHROPIC_API_KEY and MODEL_NAME placeholders
  - `.gitignore` — Python, Node, IDE, data exclusions
  - `tests/test_health.py` — health endpoint test
- All quality checks pass: ruff lint, ruff format, mypy, pytest, tsc --noEmit
- **Learnings for future iterations:**
  - Python 3.12 on this system requires venv (PEP 668 externally-managed)
  - Vite 6 create-vite template includes default assets/CSS that need cleanup
  - Tailwind CSS v4 uses `@tailwindcss/vite` plugin instead of PostCSS config
  - Backend package dirs (agents, models, services, etc.) already created with `__init__.py`
---

## 2026-03-28 - RLEX-002
- Implemented Pydantic models: Case, Argument, CourtEvaluation, MCDAResult with enums CaseStatus and JudicialLevel
- Implemented CaseFolderManager with create/load/save_state/load_state/save_iteration/load_iteration/save_output/list_cases/update_case
- Files created/changed:
  - `backend/models/case.py` — all data models
  - `backend/models/__init__.py` — re-exports
  - `backend/services/case_folder.py` — CaseFolderManager class
  - `tests/test_models.py` — 13 unit tests for model validation
  - `tests/test_case_folder.py` — 14 unit tests for folder operations
- **Learnings for future iterations:**
  - `datetime.utcnow()` is deprecated in Python 3.12 — use `datetime.now(UTC)` with `from datetime import UTC`
  - mypy `warn_return_any` catches `json.loads()` returns — annotate with explicit type and intermediate variable
  - Use `pytest` `tmp_path` fixture for CaseFolderManager tests to get isolated temp directories
  - Pydantic `model_dump_json()` / `model_validate_json()` for serialization roundtrip
---

## 2026-03-28 - RLEX-003
- Implemented case intake REST API with three endpoints: POST /api/cases, GET /api/cases, GET /api/cases/{case_id}
- CaseSubmission request model with validation (party_role pattern, min_length on title/facts)
- CaseResponse and CaseListItem response models for full vs summary views
- Proper error handling: 404 for missing cases, 422 for validation errors, 201 for creation
- Files created/changed:
  - `backend/api/cases.py` — API routes and request/response models
  - `backend/app.py` — registered cases router
  - `tests/test_cases_api.py` — 14 integration tests covering create, get, list, validation, and full flow
- **Learnings for future iterations:**
  - Use `unittest.mock.patch("backend.api.cases.folder_manager", manager)` to inject test CaseFolderManager
  - FastAPI sync TestClient works fine for these tests — no async needed
  - Pydantic Field `pattern` validates regex on strings (used for party_role: claimant|respondent)
  - Register new API routers in `backend/app.py` with `app.include_router(router, prefix="/api")`
---

## 2026-03-28 - RLEX-004
- Implemented config module (`backend/config.py`) with pydantic-settings: ANTHROPIC_API_KEY, MODEL_NAME, MAX_TOKENS, DEFAULT_TEMPERATURE, MAX_RETRIES, RETRY_BASE_DELAY
- Implemented base LLMAgent class (`backend/agents/base.py`) wrapping Anthropic Python SDK with:
  - System prompt, conversation history, configurable temperature/model/max_tokens
  - Async `call()` method using `asyncio.to_thread` for sync SDK
  - Retry logic with exponential backoff (retries on 5xx and 429, not on other 4xx)
  - Cumulative token usage tracking via `TokenUsage` dataclass
  - `call_structured()` for JSON schema-guided responses with markdown fence stripping
  - `reset_history()` and `reset_usage()` helpers
- Files created:
  - `backend/config.py` — Settings singleton
  - `backend/agents/base.py` — LLMAgent, LLMResponse, TokenUsage
  - `tests/test_agents.py` — 24 tests covering init, prompt construction, calls, retries, structured output, resets
- **Learnings for future iterations:**
  - Anthropic SDK `messages.create` is sync — wrap in `asyncio.to_thread(lambda: ...)` for async usage
  - Anthropic SDK response content blocks are a union type — need `hasattr` guard for `.text` attribute
  - mypy requires `cast(list[MessageParam], messages)` when passing `list[dict[str, str]]` to SDK
  - Mock `asyncio.sleep` via `@patch("backend.agents.base.asyncio.sleep")` to speed up retry tests
  - `anthropic.InternalServerError`, `RateLimitError`, `BadRequestError`, `APIConnectionError` are the key error types for retry testing
---

## 2026-03-28 - RLEX-005
- Implemented ClaimantAgent and RespondentAgent extending LLMAgent with structured argument generation
- Implemented convergence detector using SequenceMatcher with weighted field similarity
- Implemented ArgumentLoop orchestrator that alternates agents until convergence or max_iterations
- Added `strategy_notes` field to Argument model
- Files created/changed:
  - `backend/agents/claimant.py` — ClaimantAgent with system prompt and `generate_argument()`
  - `backend/agents/respondent.py` — RespondentAgent with system prompt and `generate_argument()`
  - `backend/services/convergence.py` — text/list similarity, argument similarity, convergence detection
  - `backend/services/argument_loop.py` — ArgumentLoop orchestrator with ArgumentLoopResult dataclass
  - `backend/models/case.py` — added `strategy_notes: list[str]` field to Argument
  - `tests/test_argument_loop.py` — 26 tests covering similarity, convergence, agents, and loop orchestration
- **Learnings for future iterations:**
  - `call_structured()` on LLMAgent handles JSON schema prompting and markdown fence stripping — use it for all structured agent outputs
  - `difflib.SequenceMatcher.ratio()` returns surprisingly high similarity for short strings with common letters — use longer, more distinctive test strings
  - `model_dump(mode="json")` serializes Pydantic models to JSON-safe dicts (handles datetime etc.) — use for `save_iteration()`
  - For tests with multiple LLM calls, create separate mock clients per agent via `_make_mock_client([resp1, resp2])` using `side_effect`
  - Ruff E501 line length limit is 100 chars — use string concatenation `()` for long system prompts instead of `\` continuation
---

## 2026-03-28 - RLEX-006
- Implemented SQLite-backed legal reference store with Pydantic models for Precedent and Law
- Keyword search (LIKE-based) and TF-IDF similarity search over precedents and laws
- Combined retrieve_precedents/retrieve_laws that merge and deduplicate keyword + TF-IDF results
- Internet fallback via httpx to DuckDuckGo API when local results < min_local_results (default 3)
- Seed script with 12 representative precedents (Hadley, Donoghue, Marbury, Miranda, etc.) and 10 statutes (UCC, GDPR, Employment Rights Act, etc.)
- Files created:
  - `backend/legal_db/models.py` — Precedent and Law Pydantic models
  - `backend/legal_db/store.py` — LegalStore class with search + fallback functions
  - `backend/legal_db/seed.py` — seed_database() with sample legal data
  - `backend/legal_db/__init__.py` — module exports
  - `tests/test_legal_db.py` — 26 tests covering models, store, search, seed, and fallback
- **Learnings for future iterations:**
  - Used sync sqlite3 instead of aiosqlite — simpler for a single-machine app with no concurrent DB writes
  - httpx response `.json()` and `.raise_for_status()` are sync methods — use `MagicMock` not `AsyncMock` for response objects
  - The `async with httpx.AsyncClient()` pattern needs: `mock_cls.return_value.__aenter__` = AsyncMock returning mock_client
  - TF-IDF stopwords must be tuned for legal text — common legal terms (e.g., "duty", "care") are NOT stopwords
  - Jurisdiction must match exactly in seeded data — "England" ≠ "United Kingdom" for Donoghue v Stevenson
- CourtAgent uses 4 sequential LLM calls (phase1→phase2→phase3→phase4/MCDA) with `reset_history()` between phases
- CourtAgent.evaluate() returns `tuple[CourtEvaluation, MCDAResult]` — always unpack both
- CourtEvaluation model has: consistency_scores, compliance_assessment, preliminary_opinion, adversarial_challenge, reconciled_decision, escalation_recommendation, escalation_decision, adversarial_review, reasoning_trace
- Use `folder_manager.save_output(case_id, "court_evaluation.json", eval.model_dump(mode="json"))` to persist court output
---

## 2026-03-28 - RLEX-007
- Implemented CourtAgent with three-phase adversarial evaluation pipeline:
  - Phase 1: Consistency analysis, compliance check, preliminary judicial opinion
  - Phase 2: Adversarial self-challenge generating counter-opinion
  - Phase 3: Reconciliation into final decision with escalation recommendation
- Updated CourtEvaluation model with new fields: preliminary_opinion, adversarial_challenge, reconciled_decision, escalation_recommendation
- Output persisted to outputs/court_evaluation.json via CaseFolderManager
- Files created/changed:
  - `backend/agents/court.py` — CourtAgent class with 3-phase evaluation, prompt builders, PHASE1/2/3 schemas
  - `backend/models/case.py` — added preliminary_opinion, adversarial_challenge, reconciled_decision, escalation_recommendation fields to CourtEvaluation
  - `tests/test_court_agent.py` — 21 tests covering all sections, prompt construction, persistence, escalation
- **Learnings for future iterations:**
  - CourtAgent uses `reset_history()` between each phase to keep prompts focused (avoids context bleed)
  - Three separate JSON schemas (PHASE1_SCHEMA, PHASE2_SCHEMA, PHASE3_SCHEMA) enforce structured output per phase
  - The adversarial_review dict aggregates data from phases 2 and 3 for backward compatibility
  - `_format_arguments()` helper serializes Argument lists into prompt-friendly text
  - Test pattern: `_make_mock_client([PHASE1_RESPONSE, PHASE2_RESPONSE, PHASE3_RESPONSE])` using `side_effect` for sequential mock responses
---

## 2026-03-28 - RLEX-008
- Implemented MCDA module with configurable weights, weighted score computation, normalization, winner prediction, and confidence estimation
- Integrated MCDA as phase 4 of CourtAgent evaluation pipeline — LLM rates each side on 5 criteria (1-10), then pure computation produces results
- MCDA results persisted to outputs/mcda_scoring.json in case folder
- Files created/changed:
  - `backend/evaluation/mcda.py` — MCDA functions: load_weights, compute_weighted_scores, determine_winner, compute_criteria_agreement, evaluate_mcda, MCDA_RATING_SCHEMA
  - `backend/evaluation/weights.json` — default weights for 5 criteria summing to 1.0
  - `backend/evaluation/__init__.py` — module exports
  - `backend/agents/court.py` — added phase 4 (MCDA rating), _build_mcda_prompt, updated return type to tuple[CourtEvaluation, MCDAResult]
  - `tests/test_mcda.py` — 26 tests covering weights, scoring, normalization, winner prediction, confidence, persistence
  - `tests/test_court_agent.py` — updated all tests for 4-phase pipeline and tuple return, added test_mcda_result_populated
- All quality checks pass: 166 tests, ruff lint/format, mypy
- **Learnings for future iterations:**
  - CourtAgent.evaluate() now returns `tuple[CourtEvaluation, MCDAResult]` — all callers must unpack both values
  - MCDA confidence = base_confidence (from score differential) * criteria_agreement (from winner consistency across criteria)
  - Raw scores are 1-10 scale, normalized to 0-1 via `(score - 1) / 9` before weighting
  - Weights loaded from `backend/evaluation/weights.json` — can be overridden by passing `weights` param
  - MCDA_RATING_SCHEMA is defined in mcda.py but used in court.py — imported there for the phase 4 LLM call
  - When adding phases to CourtAgent, update ALL_PHASES list in tests and the call_count assertion
- EscalationEngine in `backend/services/escalation.py` — check_escalation, execute_escalation, prepare_escalation_context
- Judicial hierarchy config in `backend/config/judicial_hierarchy.json` — defines levels, criteria, procedural requirements
- EscalationResult is a plain class (not Pydantic) with `to_dict()` — used for serialization in escalation records
- `_archive_level_results()` moves output JSON files to `outputs/level_{slug}/` before escalation
- Content-based escalation triggers scan reconciled_decision, adversarial_challenge, and reasoning_trace for keywords
- Escalation criteria are per-level — first_instance doesn't allow constitutional/conflicting_precedents escalation
- Frontend pages in `frontend/src/pages/`, components in `frontend/src/components/`, API client in `frontend/src/api.ts`
- react-router-dom v7 for routing — BrowserRouter in App.tsx, `useParams`/`useNavigate` hooks
- Use `import type` for type-only imports (verbatimModuleSyntax is on)
- CaseOrchestrator in `backend/orchestrator.py` — ties together ArgumentLoop, CourtAgent, EscalationEngine, LegalStore
- Orchestration API in `backend/api/orchestration.py` — POST run (202), GET status; register in `backend/app.py`
- In-memory `_progress` dict tracks orchestration phases — use `get_progress(case_id)` to query
- Sample case templates are JSON files in `backend/tools/sample_cases/` — load via `load_template(name)`
- Tools API router in `backend/api/tools.py` — register in `backend/app.py` like other routers
- CaseFolderManager constructor param is `base_path` (not `base_dir`) — use `base_path=tmp_path / "cases"` in tests
- For API tests with BackgroundTasks: use `AsyncMock` for orchestrator.run to avoid "can't await MagicMock" in TestClient
- OrchestratorResult.to_dict() produces the full output — all 10 required fields for final_result.json
- FastAPI TestClient runs BackgroundTasks synchronously — E2E tests can verify final state without polling
- Patch all 3 module-level folder_managers (cases, orchestration, tools) for full API test isolation
- Timeline endpoint constructs events from filesystem state — iteration files, output files, archived level directories
- For polling UI components, use `useRef` to persist UI state (like expanded items) across data refreshes
- Backend outputs endpoint uses `{filename:path}` to support nested paths like `level_first_instance/court_evaluation.json`
- Always add `resolve().relative_to()` path traversal guard when serving user-specified file paths
---

## 2026-03-28 - RLEX-009
- Implemented judicial hierarchy model with configurable levels and escalation criteria
- Implemented EscalationEngine with three main methods: check_escalation, execute_escalation, prepare_escalation_context
- Created judicial hierarchy JSON config with 4 levels, each with jurisdiction scope, escalation criteria, and procedural requirements
- Escalation detection via both explicit court agent recommendation and content-based keyword triggers
- Archive mechanism moves current level outputs before escalation to preserve history
- Escalation context preparation provides higher court instructions for re-running argument loop
- Files created/changed:
  - `backend/config/judicial_hierarchy.json` — 4-level hierarchy config
  - `backend/models/hierarchy.py` — EscalationCriteria, JudicialLevelConfig, JudicialHierarchy models, load_hierarchy()
  - `backend/models/__init__.py` — re-exported new hierarchy models
  - `backend/services/escalation.py` — EscalationEngine, EscalationResult, RECOMMENDATION_TO_CRITERIA mapping
  - `tests/test_escalation.py` — 28 tests covering hierarchy model, escalation result, engine logic, and full flow
- All quality checks pass: 194 tests, ruff lint/format, mypy
- **Learnings for future iterations:**
  - Escalation criteria are configured per judicial level — not all levels allow all types of escalation
  - Content trigger detection uses keyword matching on evaluation text (constitutional question, conflicting precedent, procedural irregularity)
  - Archive creates `outputs/level_{slug}/` directory and moves JSON files there before escalation
  - EscalationResult.to_dict() serializes JudicialLevel enum values via `.value`
  - `prepare_escalation_context()` returns a dict with `higher_court_instructions` string — use this to augment agent system prompts at higher levels
  - The `backend/config/` directory is new — created for judicial_hierarchy.json, can hold other config files
---

## 2026-03-28 - RLEX-010
- Implemented CaseOrchestrator in backend/orchestrator.py tying together the full Ralph Loop pipeline
- Pipeline: case intake -> argument iteration -> legal reference retrieval -> court evaluation -> MCDA scoring -> escalation check -> loop or resolve
- OrchestratorProgress tracks phase/iteration/judicial_level in-memory for status queries
- OrchestratorResult produces all 10 required output fields, saved to outputs/final_result.json
- API endpoints: POST /api/cases/{case_id}/run (202, background task), GET /api/cases/{case_id}/status
- Escalation loop re-runs argument + evaluation at higher judicial levels, capped by max_escalations
- On failure, case status resets to pending; on success, status set to completed
- Files created/changed:
  - `backend/orchestrator.py` — CaseOrchestrator, OrchestratorProgress, OrchestratorResult
  - `backend/api/orchestration.py` — run_case, get_case_status API endpoints
  - `backend/app.py` — registered orchestration router
  - `tests/test_orchestrator.py` — 27 tests covering progress, result, orchestrator pipeline, escalation, error handling, API endpoints
- All quality checks pass: 221 tests, ruff lint/format, mypy
- **Learnings for future iterations:**
  - FastAPI BackgroundTasks execute inline in TestClient — mock the orchestrator with AsyncMock to avoid "can't await MagicMock"
  - All agents can share a single mock client with `side_effect` list for sequential responses across the full pipeline
  - Escalation test needs 2x the responses (first level + second level), each with claimant/respondent + 4 court phases
  - `_progress` dict is module-level — clean up in tests with `_progress.pop(case_id, None)` to avoid cross-test contamination
  - CaseOrchestrator accepts optional LegalStore — when None, reference retrieval returns empty lists (useful for tests)
---

## 2026-03-28 - RLEX-011
- Implemented React frontend with three pages: CaseList, CaseSubmit, CaseDetail
- Case list shows all cases in a table with ID, title, status badge (color-coded), judicial level, and created date
- Case submission form with title, facts, party role selector, and supporting materials fields — POSTs to /api/cases and navigates to detail view
- Case detail page shows: case header with status, facts, iteration arguments (grouped by round with claimant/respondent side-by-side), court evaluation sections (preliminary opinion, adversarial challenge, reconciled decision), MCDA scores as comparison table, escalation history
- Added backend endpoints: GET /api/cases/{id}/iterations, GET /api/cases/{id}/outputs/{filename}
- Installed react-router-dom for client-side routing
- Created reusable StatusBadge and Nav components
- Files created/changed:
  - `frontend/src/api.ts` — typed API client with all interfaces and fetch functions
  - `frontend/src/components/Nav.tsx` — navigation bar with active link highlighting
  - `frontend/src/components/StatusBadge.tsx` — color-coded status badge component
  - `frontend/src/pages/CaseList.tsx` — case list page with table layout
  - `frontend/src/pages/CaseSubmit.tsx` — case submission form page
  - `frontend/src/pages/CaseDetail.tsx` — full case detail view with all sections
  - `frontend/src/App.tsx` — updated with BrowserRouter and routes
  - `backend/api/cases.py` — added iterations and outputs endpoints
- All quality checks pass: 221 tests, ruff lint, mypy, tsc, eslint
- **Learnings for future iterations:**
  - react-router-dom v7 is installed — use `BrowserRouter`, `Routes`, `Route` from "react-router-dom"
  - Vite proxy already configured for /api -> localhost:8000 — no CORS issues in dev
  - TypeScript strict mode + noUnusedLocals/noUnusedParameters is on — must use all imports/params
  - Backend output files are in `data/cases/{id}/outputs/` — fetch via `/api/cases/{id}/outputs/{filename}`
  - Iteration files follow pattern `round_{n}_{role}.json` — sorted alphabetically gives chronological order
  - `verbatimModuleSyntax` is on in tsconfig — use `import type` for type-only imports
---

## 2026-03-28 - RLEX-012
- Implemented real-time monitoring dashboard at /monitor with:
  - Summary bar showing case counts by status (pending, running, completed, escalated) with color coding
  - Active cases list with live status updates via 5-second polling, showing phase, iteration count, and elapsed time
  - Collapsible timeline/log view per case showing all phases (created, arguing, evaluating, scoring, completed)
  - MCDA score progression table across judicial levels
  - Escalation events highlighted with distinct orange styling and dedicated dot markers
- Backend: Added GET /api/cases/{case_id}/timeline endpoint that constructs timeline from case folder data (iterations, outputs, archived levels)
- Added "Monitor" link to navigation bar
- Files created/changed:
  - `backend/api/cases.py` — added TimelineEntry model, get_timeline endpoint, _file_mtime_iso helper
  - `frontend/src/pages/Monitor.tsx` — full monitoring dashboard page
  - `frontend/src/api.ts` — added TimelineEntry interface, getCaseTimeline(), getOutput() functions
  - `frontend/src/App.tsx` — added /monitor route
  - `frontend/src/components/Nav.tsx` — added Monitor nav link
  - `tests/test_timeline_api.py` — 10 tests covering timeline endpoint
- All quality checks pass: 231 tests, ruff lint/format, mypy, tsc, eslint
- **Learnings for future iterations:**
  - Timeline entries are constructed from file system state (iteration files, output files, archived level directories)
  - `os.path.getmtime()` provides file modification timestamps for output files that don't have embedded timestamps
  - Archived escalation levels are stored in `outputs/level_{slug}/` directories — detect these for escalation events
  - `useRef` for persisting expanded state across polling re-renders (prevents collapse on data refresh)
  - `useCallback` with `[]` deps for stable fetch function reference in `setInterval` polling
  - Manual browser verification needed for monitoring dashboard UI
---

## 2026-03-28 - RLEX-013
- Implemented sample case runner tool with CLI and API endpoint
- Created 3 sample case templates: contract dispute, employment termination, property damage
- CLI: `python -m backend.tools.run_sample [--template contract|employment|property]`
- API: POST `/api/tools/run-sample?template=contract` — creates case, triggers orchestration, returns case_id
- Added "Run Sample Case" button with template selector to Monitor dashboard
- Files created/changed:
  - `backend/tools/sample_cases/{contract,employment,property}.json` — sample templates
  - `backend/tools/run_sample.py` — CLI tool with load_template, submit_and_run, main
  - `backend/api/tools.py` — API endpoint with RunSampleResponse model
  - `backend/app.py` — registered tools router
  - `frontend/src/api.ts` — RunSampleResponse interface, runSampleCase function
  - `frontend/src/pages/Monitor.tsx` — template selector + Run Sample Case button
  - `tests/test_sample_runner.py` — 15 tests
- All quality checks pass: 246 tests, ruff lint, mypy, tsc
- **Learnings for future iterations:**
  - CaseFolderManager parameter is `base_path` not `base_dir`
  - For test fixtures that yield, annotate return type as `Generator[T]` not `T`
  - Import `_run_orchestration` from orchestration module to reuse in tools API
  - Manual browser verification needed for Run Sample Case button
---

## 2026-03-28 - RLEX-014
- Implemented end-to-end integration tests exercising full API flow: submit case, trigger orchestration, poll status, verify all 10 output fields
- 10 E2E tests covering: full flow, escalation flow, iterations/court eval, timeline, status, error handling, validation, sample runner, /docs accessibility, OpenAPI schema completeness
- Created comprehensive README.md with architecture diagram, Ralph Loop explanation, setup instructions, API usage with curl examples, judicial hierarchy and MCDA framework documentation, project structure, development commands, troubleshooting
- Added example payloads to CaseSubmission model for Swagger UI
- Verified /docs (Swagger UI) auto-generates with descriptions for all endpoints
- Files created/changed:
  - `tests/test_e2e.py` — 10 end-to-end integration tests
  - `README.md` — comprehensive project documentation
  - `backend/api/cases.py` — added json_schema_extra examples to CaseSubmission
- All quality checks pass: 256 tests, ruff lint/format, mypy, tsc
- **Learnings for future iterations:**
  - FastAPI TestClient runs BackgroundTasks synchronously — real orchestration executes inline, so E2E tests can verify final state without polling
  - Patch all three module-level folder_managers (`backend.api.cases.folder_manager`, `backend.api.orchestration.folder_manager`, `backend.api.tools.folder_manager`) for full API isolation in tests
  - `/openapi.json` endpoint returns the full schema — useful for asserting all paths are registered
  - Pydantic `model_config = {"json_schema_extra": {"examples": [...]}}` adds examples to Swagger UI
---

## 2026-03-28 - RLEX-015
- Enhanced case detail page to display ALL data from the orchestration pipeline
- Added EscalationDecision and FinalResult TypeScript interfaces to api.ts
- Updated backend outputs endpoint to support nested paths (`{filename:path}`) with path traversal protection
- New sections added to CaseDetail.tsx:
  - Outcome Summary banner (predicted winner, confidence %, judicial stage)
  - Consistency Scores with progress bars (claimant vs respondent)
  - Compliance Assessment with per-party blocks
  - Enhanced Escalation History with structured amber cards showing level transitions and triggers
  - Per-Level Evaluations with collapsible sections fetching archived level data
  - Referenced Precedents & Laws in two-column layout
  - Full Reasoning Trace as styled ordered list
- Section order: Header, Outcome Summary, Facts, Arguments, Consistency Scores, Compliance Assessment, Court Evaluation, MCDA Scoring, Escalation History, Per-Level Evaluations, Referenced Precedents & Laws, Full Reasoning Trace
- Added 3 new tests for nested path support and path traversal protection
- Files changed:
  - `frontend/src/api.ts` — added EscalationDecision, FinalResult interfaces
  - `frontend/src/pages/CaseDetail.tsx` — complete rewrite with all new sections
  - `backend/api/cases.py` — `{filename:path}` for nested output paths, path traversal guard
  - `tests/test_cases_api.py` — 3 new tests for nested/flat path output and path traversal
- All quality checks pass: 259 tests, ruff lint, mypy, tsc --noEmit
- **Learnings for future iterations:**
  - FastAPI path params use `{param:path}` syntax to capture slashes in URLs — needed for `level_first_instance/court_evaluation.json`
  - Always add path traversal protection when accepting user-provided file paths — use `resolve().relative_to()` pattern
  - `CourtEvaluation` already has `consistency_scores` and `compliance_assessment` fields — they were just not displayed before
  - `FinalResult` from `final_result.json` contains escalation_decisions as structured objects with should_escalate, reason, current_level, next_level, triggers
  - For collapsible UI sections, use simple `useState<string | null>` for tracking which item is open
  - Manual browser verification needed for all new UI sections
---
